# Інструкція з використання Docker

## Структура проекту

```
anime-spark-project/
├── data/                    # Сирі дані датасету (НЕ копіюються в Docker образ)
├── main.py                  # Головний файл програми
├── data_extraction.py       # Модуль для витягування даних
├── Dockerfile               # Конфігурація Docker образу
├── .dockerignore            # Файли, які НЕ копіюються в Docker
└── requirements.txt         # Python залежності
```

## Що таке .dockerignore?

`.dockerignore` - це файл, який вказує Docker, які файли/папки **НЕ копіювати** в контекст збірки образу.

**Навіщо це потрібно:**
- ⚡ **Прискорює збірку** - великі файли не копіюються
- 📦 **Зменшує розмір образу** - дані не потрапляють в образ
- 🔒 **Безпека** - конфіденційні файли не потрапляють в образ

**У вашому проекті:**
- `data/` - виключено, бо дані монтуються як volume при запуску
- `__pycache__/` - виключено, бо це кеш Python
- `.git/` - виключено, бо не потрібен в образі

## Де мають лежати дані?

### Поточна структура (рекомендована):
```
anime-spark-project/
└── data/                    # Дані всередині проекту
    ├── final_animedataset.csv
    └── ...
```

**Переваги:**
- ✅ Зручно для командної роботи
- ✅ Всі файли в одному місці
- ✅ Дані в `.gitignore` (не завантажуються в Git)
- ✅ Дані в `.dockerignore` (не копіюються в Docker образ)

### Альтернативна структура (якщо дані дуже великі):
```
/Users/artem/Desktop/AD/
├── anime-spark-project/     # Код проекту
└── anime-data/             # Дані поза проектом
    └── final_animedataset.csv
```

## Як запускати Docker

### Варіант 1: Використання скрипта (рекомендовано)
```bash
./run_docker.sh
```

### Варіант 2: Вручну з монтуванням volume
```bash
# Збірка образу
docker build -t my-spark-img .

# Запуск з монтуванням папки data
docker run -v "$(pwd)/data:/app/data" my-spark-img
```

### Варіант 3: Якщо дані поза проектом
```bash
docker run -v "/шлях/до/даних:/app/data" my-spark-img
```

## Важливо!

1. **Дані НЕ копіюються в Docker образ** - вони монтуються як volume
2. **Папка `data/` в `.gitignore`** - дані не завантажуються в Git
3. **Папка `data/` в `.dockerignore`** - дані не копіюються в образ
4. **При запуску контейнера дані монтуються** - вони доступні в `/app/data` всередині контейнера

## Перевірка

Щоб переконатися, що дані доступні в контейнері:
```bash
docker run -v "$(pwd)/data:/app/data" my-spark-img ls -lh /app/data
```

