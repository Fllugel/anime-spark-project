# Які файли мають бути в Git?

## ✅ Файли, які МАЮТЬ бути в Git (код, конфігурація, документація)

### Код проекту
- `main.py` - головний файл програми
- `data_extraction.py` - модуль для витягування даних
- `*.py` - всі Python файли з кодом

### Конфігурація та залежності
- `requirements.txt` - Python залежності (обов'язково!)
- `Dockerfile` - конфігурація Docker образу (для командної роботи)
- `.dockerignore` - файли, які не копіюються в Docker образ
- `.gitignore` - файли, які не завантажуються в Git

### Скрипти та документація
- `run_docker.sh` - скрипт для запуску Docker на Mac/Linux
- `run_docker.bat` - скрипт для запуску Docker на Windows
- `README.md` - основна документація проекту
- `DOCKER_USAGE.md` - інструкція з використання Docker

### Інші корисні файли
- `Dockerfile.uu` - старий Dockerfile (якщо потрібен для історії)

---

## ❌ Файли, які НЕ мають бути в Git

### Дані та результати
- `data/` - сирі дані датасету (великі файли, не завантажуються)
- `results/` - результати аналізу (CSV файли з відповідями на бізнес-питання)
- `output/` - проміжні результати

### Системні файли
- `__pycache__/` - кеш Python
- `*.pyc`, `*.pyo` - скомпільовані Python файли
- `.DS_Store` - системні файли Mac
- `Thumbs.db` - системні файли Windows

### Логи та тимчасові файли
- `*.log` - файли логів
- `logs/` - папка з логами

### Віртуальні середовища
- `venv/`, `.venv/`, `env/` - віртуальні середовища Python

### IDE та редактори
- `.idea/` - налаштування PyCharm
- `.vscode/` - налаштування VS Code (якщо не потрібні для команди)

---

## 📋 Перевірка перед комітом

Перед тим як робити коміт, перевірте:

```bash
# Подивитися що змінилося
git status

# Подивитися що буде додано
git add -n .
```

### Правильний набір файлів для коміту:

```bash
# Додати всі потрібні файли
git add .gitignore
git add .dockerignore
git add *.py
git add requirements.txt
git add Dockerfile
git add run_docker.sh
git add run_docker.bat
git add *.md
```

### НЕ додавати:
- ❌ `data/` - дані
- ❌ `results/` - результати
- ❌ `__pycache__/` - кеш
- ❌ `.DS_Store` - системні файли

---

## 🔍 Як перевірити що все правильно?

```bash
# Перевірити що в .gitignore
cat .gitignore | grep -E "(data|results|__pycache__)"

# Перевірити статус Git
git status

# Перевірити що буде закомічено
git diff --cached --name-only
```

---

## 💡 Важливо для командної роботи

1. **Dockerfile має бути в Git** - щоб всі члени команди могли зібрати образ
2. **run_docker.sh та run_docker.bat** - для різних операційних систем
3. **requirements.txt** - обов'язково! Без нього інші не зможуть встановити залежності
4. **Документація** - README.md та DOCKER_USAGE.md допоможуть команді

---

## 📝 Приклад правильного коміту

```bash
# Додати всі потрібні файли
git add .gitignore .dockerignore
git add *.py
git add requirements.txt
git add Dockerfile run_docker.sh run_docker.bat
git add *.md

# Зробити коміт
git commit -m "Додано Docker конфігурацію та оновлено код"
```

