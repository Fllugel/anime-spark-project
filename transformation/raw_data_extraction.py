"""
Заглушка для стадії витягування сирих даних (data extraction).

У майбутньому тут можна реалізувати завантаження даних з API / Kaggle / S3 тощо.
Поки що модуль лише логічно позначає існування цієї стадії.
"""

from typing import Iterable, Optional


def run_raw_data_extraction(data_path: str = "data", missing_files: Optional[Iterable[str]] = None) -> None:
    """
    Заглушка для data extraction.

    Args:
        data_path: Базовий шлях до директорії з даними.
        missing_files: Опціональний список файлів, яких не вистачає.
    """
    print("\n" + "=" * 60)
    print("🧪 DATA EXTRACTION (заглушка)")
    print("=" * 60)
    print(f"Папка даних: {data_path}")

    if missing_files:
        print("Наступні файли відсутні і мають бути створені на цій стадії:")
        for name in missing_files:
            print(f"  - {name}")

    print(
        "\nНаразі це лише заглушка. "
        "Тут можна реалізувати логіку завантаження / оновлення сирих CSV файлів."
    )


